select u_id from transfers where send_amount_currency  = 'CFA' or receive_amount_currency = 'CFA'

select count(u_id) from transfers where send_amount_currency  = 'CFA' or receive_amount_currency = 'CFA'
